node_modules/
.DS_Store
*.log
# Build-time caches (NSI fallback, etc) — not part of the shipped site.
# Exception: redirects.json is committed so CI builds and clean clones can
# skip re-resolving every known QID against Wikidata.
data/.cache/*
!data/.cache/redirects.json

# AI/LLM assistants
.claude/
.cursor/
.aider*
.continue/
.codeium/
.windsurf/
.github/copilot-*
CLAUDE.md
AGENTS.md
.specstory/
